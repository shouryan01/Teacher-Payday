import { platforms } from "@prisma/get-platform";
import escapeString from "escape-string-regexp";
const platformRegex = new RegExp("(" + [...platforms].sort((a, b) => b.length - a.length).map((p) => escapeString(p)).join("|") + ")", "g");
export {
  platformRegex
};
