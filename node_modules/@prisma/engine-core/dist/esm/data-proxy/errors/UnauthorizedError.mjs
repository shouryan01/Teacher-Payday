var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
import { DataProxyAPIError } from "./DataProxyAPIError";
import { setRetryable } from "./utils/setRetryable";
class UnauthorizedError extends DataProxyAPIError {
  constructor(info) {
    super("Unauthorized, check your connection string", setRetryable(info, false));
    this.name = "UnauthorizedError";
    this.code = "P5007";
  }
}
__name(UnauthorizedError, "UnauthorizedError");
export {
  UnauthorizedError
};
