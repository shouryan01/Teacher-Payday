var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
import { DataProxyAPIError } from "./DataProxyAPIError";
import { setRetryable } from "./utils/setRetryable";
class BadRequestError extends DataProxyAPIError {
  constructor(info) {
    super("This request could not be understood by the server", setRetryable(info, false));
    this.name = "BadRequestError";
    this.code = "P5000";
  }
}
__name(BadRequestError, "BadRequestError");
export {
  BadRequestError
};
