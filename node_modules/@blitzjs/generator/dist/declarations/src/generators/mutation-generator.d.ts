import { Generator, GeneratorOptions, SourceRootType } from "../generator";
export interface MutationGeneratorOptions extends GeneratorOptions {
    name: string;
    Name: string;
}
export declare class MutationGenerator extends Generator<MutationGeneratorOptions> {
    sourceRoot: SourceRootType;
    constructor(options: MutationGeneratorOptions);
    static subdirectory: string;
    getTemplateValues(): Promise<{
        name: string;
        Name: string;
    }>;
    getTargetDirectory(): string;
}
