import { AsyncFunc, FirstParam, PromiseReturnType } from '../types/utils';
export declare function invoke<T extends AsyncFunc, TInput = FirstParam<T>>(queryFn: T, params: TInput): Promise<PromiseReturnType<T>>;
